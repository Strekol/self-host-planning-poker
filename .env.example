# Exemple de fichier .env pour Planning Poker
# Copiez ce fichier vers .env et modifiez les valeurs selon vos besoins

# =============================================================================
# CONFIGURATION DE BASE DE DONNÉES
# =============================================================================

# Option 1: Configuration par URL (recommandée)
# Décommentez UNE des lignes suivantes selon votre base de données

# PostgreSQL
# DATABASE_URL=postgresql://username:password@localhost:5432/planning_poker

# MySQL
# DATABASE_URL=mysql://username:password@localhost:3306/planning_poker

# SQLite (par défaut)
# DATABASE_URL=sqlite:///./database.db

# Option 2: Configuration par variables séparées
# Utilisez cette méthode si vous préférez séparer les paramètres

# Type de base de données: sqlite, postgresql, mysql
DATABASE_TYPE=sqlite

# Pour PostgreSQL et MySQL
# DATABASE_HOST=localhost
# DATABASE_PORT=5432  # 5432 pour PostgreSQL, 3306 pour MySQL
# DATABASE_NAME=planning_poker
# DATABASE_USER=planning_poker_user
# DATABASE_PASSWORD=your_password_here

# Options spécifiques PostgreSQL
# DATABASE_SSL_MODE=prefer  # require, prefer, disable

# Options spécifiques MySQL
# DATABASE_CHARSET=utf8mb4

# Pour SQLite uniquement
DATABASE_PATH=./development.db

# =============================================================================
# CONFIGURATION FLASK
# =============================================================================

# Mode debug (true pour développement, false pour production)
FLASK_DEBUG=true

# Clé secrète Flask (générez une clé sécurisée pour la production)
SECRET_KEY=dev-secret-key-change-in-production

# =============================================================================
# EXEMPLES DE CONFIGURATION POUR DIFFÉRENTS ENVIRONNEMENTS
# =============================================================================

# Développement local avec PostgreSQL Docker
# docker run --name postgres-dev -e POSTGRES_PASSWORD=devpass -e POSTGRES_DB=planning_poker -p 5432:5432 -d postgres:13
# DATABASE_URL=postgresql://postgres:devpass@localhost:5432/planning_poker

# Développement local avec MySQL Docker  
# docker run --name mysql-dev -e MYSQL_ROOT_PASSWORD=devpass -e MYSQL_DATABASE=planning_poker -p 3306:3306 -d mysql:8.0
# DATABASE_URL=mysql://root:devpass@localhost:3306/planning_poker

# Production avec base de données managée
# DATABASE_URL=postgresql://prod_user:secure_password@db.example.com:5432/planning_poker_prod
